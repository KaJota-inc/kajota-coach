# Intermediates generated by stitch.sh and the VO pre-processing pipeline.
# Original source m4a lives outside the repo (recording tool's default
# location); the processed vo-raw.wav that stitch.sh consumes IS tracked.
vo-raw-original.wav
vo-trimmed.wav
concat.txt
